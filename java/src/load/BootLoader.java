package load;

import java.net.URL;

public class BootLoader {

	/*public static void ShowBootstrap()
	{
		URL[] urls = sun.misc.Launcher.getBootstrapClassPath().getURLs();
		for(int i=0;i<urls.length;i++)
		{
			System.out.println(urls[i].toExternalForm());
		}
		
	}*/
}
